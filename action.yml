name: Puppeteer Container
description: A GitHub Action / Docker image for Puppeteer, the Headless Chrome Node API
author: BVM
runs:
  using: docker
  image: Dockerfile
branding:
  icon: globe
  color: green
